import React,{createContext} from 'react';

 let InputOneData = createContext([]);
 export let InputTwoData = createContext(0);
 export let LabelValues = createContext("Input Value");

 export  default InputOneData;